import React from "react";
import "./Tarifs.scss";
import useRemoteContents from "scripts/hooks/useRemoteContents";

const Tarifs = props => {
  //const { contents } = useRemoteContents(`/api/contents/tarifs`);

  return <div className="TarifsAdmin" />;
};

export default Tarifs;
