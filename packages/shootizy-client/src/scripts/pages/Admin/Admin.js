import React from "react";
import { Helmet } from "react-helmet";

const Admin = () => {
  return (
    <div>
      <Helmet>
        <title>Admin</title>
      </Helmet>
      Are you sure you are admin ?
    </div>
  );
};

export default Admin;
